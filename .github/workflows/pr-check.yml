name: PR Check

on:
  pull_request:
    branches: [main]

jobs:
  pr-check:
    runs-on: ubuntu-slim

    steps:
      - name: Print branch name
        run: echo "PR check running for branch: ${{ github.head_ref }}"
